import React, { createContext } from 'react';

export const myContext = createContext(null)

const conextProvider = () => {

}

const AppContext = () => {
    return (
        <div>

        </div>
    );
};

export default AppContext;